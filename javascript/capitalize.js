// capitalize(): Will capitalize the first character of a string and return it.
function capitalize(nString){
    return nString = nString.replace(nString[0], nString[0].toUpperCase());
}

module.exports = capitalize;